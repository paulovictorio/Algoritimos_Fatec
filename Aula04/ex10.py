from math import pow,sqrt

n = float(input("Entre com um número maior que 0: "))
quadrado = pow(n, 2)
cubo = pow(n, 3)
raiz = sqrt(n)

print(f"{n} ao quadrado é {quadrado}, ao cubo é {cubo} e a raiz quadrada é {raiz}")